<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description, input_html: {maxlength: 50, style: 'width: 50%'} %>
  <%= f.association :ingredient, input_html: {style: 'width: 50%'} %>
  <%= f.submit "Submit", class: "btn btn-primary"%>
<% end %>
